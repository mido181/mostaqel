import { LastnameshorterPipe } from './lastnameshorter.pipe';

describe('LastnameshorterPipe', () => {
  it('create an instance', () => {
    const pipe = new LastnameshorterPipe();
    expect(pipe).toBeTruthy();
  });
});
