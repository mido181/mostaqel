import { GetlengthDirective } from './getlength.directive';

describe('GetlengthDirective', () => {
  it('should create an instance', () => {
    const directive = new GetlengthDirective();
    expect(directive).toBeTruthy();
  });
});
