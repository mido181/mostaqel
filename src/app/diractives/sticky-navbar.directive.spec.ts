import { StickyNavbarDirective } from './sticky-navbar.directive';

describe('StickyNavbarDirective', () => {
  it('should create an instance', () => {
    const directive = new StickyNavbarDirective();
    expect(directive).toBeTruthy();
  });
});
